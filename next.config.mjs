/** @type {import('next').Config} */
const nextConfig = {
  output: 'export',
  basePath: '/Lahari-Portfolio', // MUST match your GitHub repo name exactly
  assetPrefix: '/Lahari-Portfolio/', 
  images: { unoptimized: true },
};
export default nextConfig;
